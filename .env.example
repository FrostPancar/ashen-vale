# WebSocket URL for online co-op (set on Vercel at build time).
# Example after deploying server.js to Railway:
# VITE_WS_URL=wss://ashen-vale-coop.up.railway.app

# Cursor API key for the pixel art generator — bills against your subscription credits.
# Create at https://cursor.com/dashboard/integrations
# CURSOR_API_KEY=cursor_...
